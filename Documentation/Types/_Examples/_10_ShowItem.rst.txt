
..  _types-example-showitem:
..  _types-example-showitem-3-fields:

Display all fields on one page of the form
------------------------------------------

The table containing the blog comments does not contain many fields. So they can
all be displayed on one page of the backend form without using tabs:

..  literalinclude:: _CodeSnippets/_tx_blogexample_comment_showitem.php
    :caption: EXT:blog_example/Configuration/TCA/tx_blogexample_comment.php

..  _types-examples-required-tabs:
..  _types-example-showitem-2-tabs:
..  _types-example-showitem-rename-general:

Group fields using tabs and use palettes
----------------------------------------

If a table has a larger number of fields, grouping them into tabs and palettes
can improves the backend user experience. Palettes have the additional value
that they can be reused across different record types.

..  literalinclude:: _CodeSnippets/_tx_blogexample_post_tabs.php
    :caption: EXT:blog_example/Configuration/TCA/tx_blogexample_post.php

The syntax for tabs is:

`--div--;<label>`

Where label can be plain text label or `label reference <https://docs.typo3.org/permalink/t3coreapi:label-reference>`_ (preferred).

The syntax to insert a palette is:

`--palette--;<label>;<paletteName>`

The label can be omitted.

..  seealso::
    `Grouping fields (palettes) <https://docs.typo3.org/permalink/t3tca:palettes>`_
