..  _ctrl-reference-adminonly:

..  confval:: adminOnly
    :name: ctrl-adminOnly
    :Path: $GLOBALS['TCA'][$table]['ctrl']['adminOnly']
    :type: boolean
    :Scope: Display / Proc.

    Records can be changed  *only* by "admin"-users (having the "admin" flag set).

    ..  rubric:: Example: Table `my_table` is only editable by admin users:

    ..  code-block:: php
        :caption: EXT:my_sitepackage/Configuration/TCA/my_table.php

        'ctrl' => [
            'adminOnly' => 1,
            ...
        ],
