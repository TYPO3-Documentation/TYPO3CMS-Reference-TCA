foreign\_label
~~~~~~~~~~~~~~

:aspect:`Datatype`
    string

:aspect:`Scope`
    Display / Proc.

:aspect:`Description`
    If set, it overrides the label set in :php:`$GLOBALS['TCA'][<foreign_table>]['ctrl']['label']` for the inline-view.
