..  _columns-link-properties-valuePicker:

..  confval:: valuePicker
    :name: link-valuePicker
    :Path: $GLOBALS['TCA'][$table]['columns'][$field]['config']
    :type: array
    :Scope: Display

    Renders a select box with static values next to the input field. When an
    item is selected, the value replaces the current input value.

    ..  versionchanged:: 14.0
        It is now recommended to define associative array keys for
        `items` using `label` and `value`. Existing numeric
        arrays are automatically migrated during TCA processing.

        The `mode` option no longer supports `append` or
        `prepend`. The Value Picker always behaves like a replace operation.
        Legacy `mode` settings are ignored. See also
        `Breaking: #107677 - Drop prepend and append modes from TCA Value Picker <https://docs.typo3.org/permalink/changelog:breaking-107677-1760339783>`_.


    items (array)
        A list of selectable items. Each item should be an associative array:
        `['label' => 'Shown label', 'value' => 'storedValue']`.
        The label can be plain text or a
        `label reference <https://docs.typo3.org/permalink/t3coreapi:label-reference>`_.

    ..  hint::
        In earlier TYPO3 versions, the `mode` option allowed you to
        `prepend` or `append` the selected value to the current input.
        This option was removed in TYPO3 v14.0. The value picker now always
        **replaces** the current field value.

        If you previously used `mode => 'prepend'` to insert a prefix (for
        example, to automatically add `https://` before a link), you can achieve a
        similar result with a custom
        :ref:`FormEngine element <t3coreapi:FormEngine-Rendering>` or a small
        JavaScript hook that modifies the input value after selection.

..  rubric:: Example: A link field with value picker

..  literalinclude:: _Snippets/_valuePicker.php
