
..  confval:: exclusiveKeys
    :name: select-tree-exclusiveKeys
    :Path: $GLOBALS['TCA'][$table]['columns'][$field]['config']
    :type: string (list of)
    :Scope: Display  / Proc.

    List of keys that exclude any other keys in a select box where multiple
    items could be selected.
