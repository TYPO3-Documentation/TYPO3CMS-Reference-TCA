..  _columns-password-properties-default:

..  confval:: default
    :name: password-default
    :Path: $GLOBALS['TCA'][$table]['columns'][$field]['config']['default']
    :type: string
    :Scope: Display  / Proc.

    Default value set if a new record is created. If empty, no password gets
    selected.
