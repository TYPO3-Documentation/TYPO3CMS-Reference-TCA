..  confval:: multiple
    :name: folder-multiple
    :Path: $GLOBALS['TCA'][$table]['columns'][$field]['config']['multiple']
    :type: boolean
    :Scope: Display / Proc.

    Allows the *same item* more than once in a list.
