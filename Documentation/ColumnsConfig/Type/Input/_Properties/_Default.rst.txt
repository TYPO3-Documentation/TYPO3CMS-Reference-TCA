..  _columns-input-properties-default:

..  confval:: default
    :name: input-default
    :Path: $GLOBALS['TCA'][$table]['columns'][$field]['config']['default']
    :type: string
    :Scope: Display  / Proc.

    Default value set if a new record is created. If empty, no input gets
    selected.
